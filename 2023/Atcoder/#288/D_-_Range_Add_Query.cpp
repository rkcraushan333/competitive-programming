#include <bits/stdc++.h>
using namespace std;
#define int long long

// by inforkc
signed main()
{
    int n, k, q;
    cin >> n >> k;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
        cin >> v[i];
    cin >> q;
    while (q--)
    {
        int a, b;
        cin >> a >> b;
    }
    return 0;
}